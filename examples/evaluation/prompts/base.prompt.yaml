apiVersion: promptfarm/v1
kind: Prompt

metadata:
  id: base
  version: 1.0.0
  title: Base guidance

spec:
  artifact:
    type: instruction

  inputs:
    - name: system_name
      type: string
      required: true

  messages:
    - role: system
      content: |
        You are a pragmatic architect.
        Keep recommendations concrete and verifiable.

  use: []
